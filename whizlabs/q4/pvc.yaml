# PVC will bind the Deployment from q2 to the PersistentVolume in q4
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: data-volume-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  volumeName: data-volume
  storageClassName: fast-storage
